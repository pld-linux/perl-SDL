diff -durN SDL_perl-1.18.orig/Makefile.linux SDL_perl-1.18/Makefile.linux
--- SDL_perl-1.18.orig/Makefile.linux	Sat May 25 22:41:47 2002
+++ SDL_perl-1.18/Makefile.linux	Mon May 27 09:34:50 2002
@@ -42,19 +42,20 @@
 #
 
 %ext = ( 
-	SDL_image => { inc => 'HAVE_SDL_IMAGE', test => 'SDL_image.h' },
-	SDL_mixer => { inc => 'HAVE_SDL_MIXER', test => 'SDL_mixer.h' },
-	SDL_net => { inc => 'HAVE_SDL_NET', test => 'SDL_net.h' },
-	SDL_ttf => { inc => 'HAVE_SDL_TTF', test => 'SDL_ttf.h' },
-	SDL_gfx => { inc => 'HAVE_SDL_GFX', test => 'SDL_gfxPrimitives.h' },
+	SDL_image => { inc => 'HAVE_SDL_IMAGE', test => 'SDL_image.h', exists=>1 },
+	SDL_mixer => { inc => 'HAVE_SDL_MIXER', test => 'SDL_mixer.h', exists=>1 },
+	SDL_net => { inc => 'HAVE_SDL_NET', test => 'SDL_net.h', exists=>1 },
+	SDL_ttf => { inc => 'HAVE_SDL_TTF', test => 'SDL_ttf.h', exists=>1 },
+	SDL_gfx => { inc => 'HAVE_SDL_GFX', test => 'SDL_gfxPrimitives.h', exists=>1 },
 	SDL_console => { inc => 'HAVE_SDL_CONSOLE', test => 'CON_console.h' },
-	png => { inc => 'HAVE_PNG', test => 'png.h' },
-	jpeg => { inc => 'HAVE_JPEG', test => 'jpeglib.h' },
-	GL => { inc => 'HAVE_GL', test => 'gl.h' },
-	GLU => { inc => 'HAVE_GLU', test => 'glu.h' },
-	smpeg => { inc => 'HAVE_SMPEG', test => 'smpeg.h' },
+	png => { inc => 'HAVE_PNG', test => 'png.h', exists=>1 },
+	jpeg => { inc => 'HAVE_JPEG', test => 'jpeglib.h', exists=>1 },
+	GL => { inc => 'HAVE_GL', test => 'gl.h', exists=>1 },
+	GLU => { inc => 'HAVE_GLU', test => 'glu.h', exists=>1 },
+	smpeg => { inc => 'HAVE_SMPEG', test => 'smpeg.h', exists=>1 },
 );
 
+=cut
 #
 # Locate optional packages
 #
@@ -73,6 +74,7 @@
 for $e ( sort keys %ext ) {
 	found_mod ($e,$ext{$e}{exists});
 }
+=cut
 
 #
 # Get GLU version
@@ -80,7 +82,7 @@
 
 if  ( $ext{GLU}{exists} ) {
 	print "Detecting GLU Version\n";
-	system ("gcc -o detect detect.c -lGLU -lGL $sdl_libs");
+	system ("gcc -o detect detect.c -I/usr/X11R6/include -L/usr/X11R6/lib -lGLU -lGL $sdl_libs");
 	my $version = `./detect` * 10;
 	push @defs, "-DHAVE_GLU_VERSION=$version";
 	system ("rm detect");
